# Tested with: pandas 3.0.5, numpy 2.4.6, scikit-learn 1.9.0, shap 0.52.0,
# statsmodels 0.14.6, matplotlib 3.11.1 (Python 3.14, Linux).
# Minimum versions below are set conservatively for compatibility with
# common Anaconda distributions (Python >=3.10); pandas>=2.0 is required
# specifically for the `format="mixed"` argument to pd.to_datetime used in
# src/analysis/*, src/modeling/*, src/validation/* when re-reading the
# master panel CSV (raw source dates have inconsistent granularity across
# the 4 datasets, e.g. "2019-01-31" vs "2019-01-31 00:00:00" vs
# "2019-01-02 09:10:08").
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
shap>=0.44
statsmodels>=0.14
matplotlib>=3.7
pytest>=7.4
networkx>=3.0
plotly>=5.20
# Phase 1 only (src/data_engineering/build_raw_from_drive.py): fetches the raw
# FOI / Contracts Finder archive from the project's public Google Drive folder.
# Tested with gdown 6.1.0. Not required if you place the four
# data/raw/*_clean.csv files manually.
gdown>=5.1
# Needed for src/data_engineering/build_raw_from_drive.py to read Bradford's
# Jan-19 return, which is published only as a macro-enabled Excel (.xlsm)
# file with no CSV equivalent. Without this, that one month (101 rows) is
# silently skipped with a warning rather than failing loudly.
openpyxl>=3.1
